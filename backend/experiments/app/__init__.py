"""TickLab experiments application package (Task 2.6).

Makes `backend/experiments/app/` importable so template VALIDATE
(docs/08 §8.4, "imports resolve") can import strategy files as
`experiments.app.templates.{template_id}` with `--app-dir backend`.
"""
